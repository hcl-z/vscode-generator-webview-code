
/**
 * @typedef {Object} Config
 * @property {Array.<string|{from: string, to: string}>} [config.path] - 路径配置数组
 * @property {Array.<string|{from: string, to: string}>} [config.templatePath] - 模板路径配置数组
 * @property {Array<string>} [config.exclude] - 要排除的文件或文件夹
 * @property {Boolean} [config.copyRoot]
 */


/**
 * @type {Config}
 */
export default {
    path: [],
}
